// index.js

// A simple function
function greet(name) {
  console.log(`Hello, ${name}!`);
}

// Call the function
greet("Shikhar");

// Export if needed (for modular projects)
module.exports = { greet };
